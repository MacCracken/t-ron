# Changelog

All notable changes to t-ron are documented here.

## [0.1.0] — 2026-03-22

### Added
- Initial scaffold: security gate middleware, policy engine (per-agent ACLs with glob matching), rate limiter (token bucket), payload scanner (SQL/shell/template injection + path traversal detection), pattern analyzer (tool enumeration detection), agent risk scoring, audit logger, T.Ron query API for SecureYeoman personality
